apiVersion: v1
kind: Service
metadata:
  name: go-guardian
  labels:
    app: go-guardian
spec:
  type: ClusterIP
  selector:
    app: go-guardian
  ports:
  - name: mcp
    port: 3000
    targetPort: mcp
    protocol: TCP
